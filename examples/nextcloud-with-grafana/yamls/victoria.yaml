global:
  scrape_interval: 15s
  scrape_timeout: 10s

scrape_configs:
  - job_name: traefik
    metrics_path: /metrics
    static_configs:
      - targets:
          - 'traefik-hub.kube-system:9100'

