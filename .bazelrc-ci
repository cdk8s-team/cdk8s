# Bazel configuration that will be copied to /etc/bazel.bazelrc.
# This allows us adding specific configuration flags for builds within GitHub Actions.
# See more: https://docs.bazel.build/versions/master/guide.html#where-are-the-bazelrc-files

# Print all enabled Bazel flags in CI mode. This makes it easier to debug and reproduce
# Bazel issues that show up on CircleCI.
common --announce_rc

# Save downloaded repositories in a location that can be cached by CircleCI. This helps us
# speeding up the analysis time significantly with Bazel managed node dependencies on the CI.
build --repository_cache=$HOME/bazel_repository_cache

# Workaround https://github.com/bazelbuild/bazel/issues/3645. Bazel doesn't calculate the
# memory ceiling correctly when running under Docker.
build --local_ram_resources=6145
build --local_cpu_resources=2
