{
  "name": "{{ $base }}",
  "version": "1.0.0",
  "main": "main.js",
  "types": "main.ts",
  "license": "Apache-2.0",
  "private": true,
  "scripts": {
    "import": "cdk8s import k8s",
    "compile": "tsc",
    "test": "echo ok",
    "build": "yarn run import && yarn compile && yarn test",
    "watch": "tsc -w",
    "synth": "cdk8s synth --app 'node main.js'"
  }
}
