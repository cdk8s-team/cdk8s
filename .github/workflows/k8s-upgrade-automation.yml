# ~~ Generated by projen. To modify, edit .projenrc.ts and run "npx projen".

name: k8s-upgrade-automation
on:
  schedule:
    - cron: 0 18 * * 0
  workflow_dispatch: {}
jobs:
  check-latest-k8s-release:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - name: Get latest K8s Release
        id: k8s-latest-release
        uses: pozetroninc/github-action-get-latest-release@master
        with:
          repository: kubernetes/kubernetes
          excludes: prerelease, draft
      - id: check-release-version
        run: pozetroninc/github-action-get-latest-release@master
        with:
          repository: kubernetes/kubernetes
          excludes: prerelease, draft
  generate-k8s-spec:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Setup Node.js
        uses: actions/setup-node@v2
        with:
          node-version: 18.12.0
      - name: Install dependencies
        run: yarn install --check-files
      - name: Generate Kubernetes schema
        env:
          GITHUB_TOKEN: ${{ secrets.PROJEN_GITHUB_TOKEN }}
        run: npx ts-node ${{ github.workspace }}/tools/import-spec.sh 1.27.0
        continue-on-error: false
      - name: Create Pull Request
        id: create-pr
        uses: peter-evans/create-pull-request@v4
        with:
          token: ${{ secrets.PROJEN_GITHUB_TOKEN }}
          commit-message: |-
            chore: v1.27.0 kubernetes-spec

            This PR is adds the v1.27.0 Kubernetes spec. This is required in order for us to add a new version to cdk8s-plus. See details in [workflow run].

            [Workflow Run]: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}

            ------

            *Automatically created by projen via the "create-pull-request" workflow*
          branch: github-actions/generate-k8s-spec-1.27.0
          title: "chore: v1.27.0 kubernetes-spec"
          labels: auto-approve
          body: |-
            This PR is adds the v1.27.0 Kubernetes spec. This is required in order for us to add a new version to cdk8s-plus. See details in [workflow run].

            [Workflow Run]: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}

            ------

            *Automatically created by projen via the "create-pull-request" workflow*
          author: github-actions <github-actions@github.com>
          committer: github-actions <github-actions@github.com>
          signoff: true
  create-go-branch:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - name: Checkout
        uses: actions/checkout@v2
        with:
          repository: cdk8s-team/cdk8s-plus-go
      - name: Create new branch
        env:
          GITHUB_TOKEN: ${{ secrets.PROJEN_GITHUB_TOKEN }}
        run: git checkout -b k8s.27
        continue-on-error: false
      - name: Push new branch
        env:
          GITHUB_TOKEN: ${{ secrets.PROJEN_GITHUB_TOKEN }}
        run: git push --set-upstream origin k8s.27
        continue-on-error: false
  create-backport-label:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - name: Checkout
        uses: actions/checkout@v2
        with:
          repository: cdk8s-team/cdk8s-plus
      - name: Create new backport label for old version
        uses: actions-ecosystem/action-add-labels@v1
        with:
          labels: backport-to-k8s-26/main
          repo: cdk8s-team/cdk8s-plus
  create-new-plus-branch:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - name: Checkout
        uses: actions/checkout@v2
        with:
          repo: cdk8s-team/cdk8s-plus
      - name: Create new branch
        uses: peterjgrainger/action-create-branch@v2.2.0
        env:
          GITHUB_TOKEN: ${{ secrets.PROJEN_GITHUB_TOKEN }}
        with:
          branch: k8s-27/main
          sha: ${{ github.event.pull_request.head.sha }}
      - name: Update projen and README references to latest k8s version
        env:
          GITHUB_TOKEN: ${{ secrets.PROJEN_GITHUB_TOKEN }}
        run: git push --set-upstream origin k8s.27
        continue-on-error: false
      - name: Setup Node.js
        uses: actions/setup-node@v2
        with:
          node-version: 18.12.0
      - name: Install dependencies
        run: yarn install --check-files
      - name: Import the new k8s spec from the prerequisite step
        run: yarn run import
      - name: Start local kubernetes cluster
        run: yarn run import
      - name: Generate API types from the local Kubernetes cluster
        run: yarn regenerate-api-information
      - name: Let projen update the remaining files
        run: yarn build
      - name: Update references in docs/plus/**
        env:
          GITHUB_TOKEN: ${{ secrets.PROJEN_GITHUB_TOKEN }}
        run: git push --set-upstream origin k8s.27
        continue-on-error: false
      - name: Push the branch and verify that automation builds/tags/releases the new version successfully.
        env:
          GITHUB_TOKEN: ${{ secrets.PROJEN_GITHUB_TOKEN }}
        run: git push --set-upstream origin k8s-27/main
        continue-on-error: false
      - name: Set new branch as default branch for repo
  update-cdk8s-website:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Create new branch
        uses: peterjgrainger/action-create-branch@v2.2.0
        env:
          GITHUB_TOKEN: ${{ secrets.PROJEN_GITHUB_TOKEN }}
        with:
          branch: cdk8s-upgrade/v1.27.0
          sha: ${{ github.event.pull_request.head.sha }}
      - name: Update references to newest k8s version in cdk8s repo
        env:
          GITHUB_TOKEN: ${{ secrets.PROJEN_GITHUB_TOKEN }}
        run: git push --set-upstream origin k8s.27
        continue-on-error: false
      - name: Let projen update the remaining files
        run: yarn build
      - name: Update references in docs/** with new syntax
        env:
          GITHUB_TOKEN: ${{ secrets.PROJEN_GITHUB_TOKEN }}
        run: git push --set-upstream origin k8s.27
        continue-on-error: false
      - name: Create Pull Request
        id: create-pr
        uses: peter-evans/create-pull-request@v4
        with:
          token: ${{ secrets.PROJEN_GITHUB_TOKEN }}
          commit-message: |-
            chore(website): cdk8s-plus-27

            This PR updates the website with the latest version of cdk8s-plus. See details in [workflow run].

            [Workflow Run]: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}

            ------

            *Automatically created by projen via the "create-pull-request" workflow*
          branch: github-actions/website-update-1.27.0
          title: "chore(website): cdk8s-plus-27"
          body: |-
            This PR updates the website with the latest version of cdk8s-plus. See details in [workflow run].

            [Workflow Run]: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}

            ------

            *Automatically created by projen via the "create-pull-request" workflow*
          author: github-actions <github-actions@github.com>
          committer: github-actions <github-actions@github.com>
          signoff: true
