{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "lerna run build",
    "test": "lerna run test",
    "package": "lerna run package"
  },
  "workspaces": [ 
    "packages/*",
    "examples"
  ],
  "devDependencies": {
    "lerna": "^3.16.4"
  }
}
