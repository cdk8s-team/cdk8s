{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "lerna run build",
    "test": "lerna run test",
    "package": "lerna run package && tools/collect-dist.sh"
  },
  "workspaces": [ 
    "packages/*",
    "examples"
  ],
  "devDependencies": {
    "lerna": "^3.20.2"
  }
}
